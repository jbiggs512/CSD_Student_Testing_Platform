# Auth route handlers. Wire up requests to the service layer.
# Example:
# @router.post("/login", response_model=schemas.TokenResponse)
# def login(credentials: schemas.LoginRequest):
#     ...

from fastapi import APIRouter

router = APIRouter()
